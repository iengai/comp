version: '3.8'

services:
  go-builder:
    build:
      context: .
      dockerfile: Dockerfile.build
    volumes:
      - .:/app
    working_dir: /app
    environment:
      SERVICE: "${SERVICE:?SERVICE is required}"
    command: ["bash", "-c", "./build-and-zip.sh $SERVICE"]
